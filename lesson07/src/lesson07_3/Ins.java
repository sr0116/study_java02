package lesson07_3;

public class Ins {
	public static void main(String[] args) {
		String str = new String();
		//1.String : 주소상으로는 클래스, 문법 상으로는 데이터 타입
		//2. str = 지역 변수, 객체이자 변수
		//3. =: 대입 연산자(할당의 기능)
		//4. new : 연산자, 인스턴스 생성 트리거 , 생성자를 필요로 함
		//5. String(): 생성자 호출문구, 기본 생성자
		// 4+5 : 새로운 인스턴스 생성 (객체라고 생각해도 됨)
		
		//str.length();
		//new String().length(); 둘다 가능 (5번하고 14번만 인스턴스 존재하고 나머지는 없음)
		
//		String s; 객체임
//		String s2= null; 객체임 인스턴스는 없음
		
	}

}
